<%- include('../partials.header') %>

<section id="show-page">
    <div>Title: </div>
    <div><%= skatespot.title %></div>
    <div>Location: </div>
    <div><%= skatespot.location %></div>
    <div>Rating: </div>
    <div><%= skatespot.rating %></div>
</section>  
<a href="/skatespots/new">New Spot</a>

<a href="/skatespots/<%= skatespot.id %>/edit">Edit Spot</a>
<!-- <a href="/skatespots/<%= skatespot.id %>/edit">Edit Skatespot</a> -->


<!-- <table>
    <thead>
        <tr>
            <th>User</th>
            <th>Date</th>
            <th>Review</th>
            <th>Rating</th>
        </tr>
    </thead>
    <tbody>
        <% let total = 0 %>
        <% skatespot.reviews.forEach(function(r) { %>
            <% total += r.rating %>
            <tr>
                <td class="review-user"<img alt="avatar" src="<%= r.userAvatar %> referrerpolicy="no-referrer" ><%= r.userName %></td>
                <td><%= r.createdAt.toLocaleDateString() %></td>
                <td><%= r.description %></td>
                <td><%= r.content %></td>
                <td><%= r.rating %></td>
                <td></td>
                <% if (user?._id.equals(r.user)) { %>
                    <form action="/reviews/<%= r._id %>?_method=DELETE" method="POST">
                        <button type="submit">X</button>
                    </form>
                    <% } %>
                </tr>
                <% }); %>
                <tr>
                    <td colspan="3"></td>
                    <td><strong><%= (total / skatespot.reviews.length).toFixed(1) %></strong></td>
                </tr>
            </tbody>
        </table> -->
<%- include('../partials/footer') %>